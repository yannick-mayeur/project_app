<div id="<%= "follow_form_#{@course.id}" %>">
  <% if current_duser.courses.exists?(@course.id) %>
    <%= render 'courses/unfollow' %>
  <% else %>
    <%= render 'courses/follow' %>
  <% end %>
</div>
